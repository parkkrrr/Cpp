#include <iostream>

using namespace std;

struct Node{
    int data;
    struct Node *next;
};

int main(){
    struct Node *node;

    struct Node *newNode=(struct Node*) malloc(sizeof(struct Node));

    


    return 0;
}