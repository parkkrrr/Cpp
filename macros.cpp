#include <bits/stdc++.h>
using std::cin;
using std::cout;

#define multiply(a, b) (a * b)

void macros(int a, int b)
{
    cout << multiply(a, b);
}
int main()
{
    int a, b;
    cin >> a >> b;
    macros(a, b);

    return 0;
}
